From 32e13a9d396f1c197daa6c8a1aae94cb14a4f74e Mon Sep 17 00:00:00 2001
From: Jake Bailey <5341706+jakebailey@users.noreply.github.com>
Date: Thu, 7 Apr 2022 10:36:47 -0700
Subject: [PATCH 6/6] Reduce test tasks down for now

---
 Gulpfile.js | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/Gulpfile.js b/Gulpfile.js
index edf1cebb00..58fa587e97 100644
--- a/Gulpfile.js
+++ b/Gulpfile.js
@@ -447,7 +447,7 @@ preTest.displayName = "preTest";
 const postTest = (done) => cmdLineOptions.lint ? lint(done) : done();
 
 const runTests = () => runConsoleTests(testRoot, "mocha-fivemat-progress-reporter", /*runInParallel*/ false, /*watchMode*/ false);
-task("runtests", series(preBuild, preTest, runTests, postTest));
+task("runtests", series(/*preBuild, preTest,*/ runTests /*, postTest*/));
 task("runtests").description = "Runs the tests using the built run.js file.";
 task("runtests").flags = {
     "-t --tests=<regex>": "Pattern for tests to run.",
@@ -467,7 +467,7 @@ task("runtests").flags = {
 };
 
 const runTestsParallel = () => runConsoleTests(testRoot, "min", /*runInParallel*/ cmdLineOptions.workers > 1, /*watchMode*/ false);
-task("runtests-parallel", series(preBuild, preTest, runTestsParallel, postTest));
+task("runtests-parallel", series(runTestsParallel));
 task("runtests-parallel").description = "Runs all the tests in parallel using the built run.js file.";
 task("runtests-parallel").flags = {
     "   --no-lint": "disables lint.",
-- 
2.35.1

