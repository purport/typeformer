{
    "extends": "@tsconfig/node14",
    "compilerOptions": {
        "incremental": true,
        "outDir": "./dist",
        "sourceMap": true,
        "module": "Node16" // Technically node14+ but that's okay. NodeNext would be a moving target.
    },
    "include": ["src/**/*.ts"]
}
